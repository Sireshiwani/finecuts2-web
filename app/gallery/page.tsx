import PageShell from "@/components/page-shell";
import Gallery from "@/components/gallery";

export default function GalleryPage() {
  return (
    <PageShell
      title="Gallery"
      subtitle="Real transformations from the chair — fades, locs, and full grooming sessions."
    >
      <Gallery />
    </PageShell>
  );
}
